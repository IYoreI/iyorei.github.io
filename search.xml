<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>git使用指南</title>
      <link href="post/undefined.html"/>
      <url>post/undefined.html</url>
      
        <content type="html"><![CDATA[<h1 id="git使用配置记录："><a href="#git使用配置记录：" class="headerlink" title="git使用配置记录："></a>git使用配置记录：</h1><h2 id="1-初始配置项："><a href="#1-初始配置项：" class="headerlink" title="1. 初始配置项："></a>1. 初始配置项：</h2><ol><li><p>修改本地bash路径（不使用默认的）</p><p>针对git2.0以上，“环境变量”中用户变量添加一个“Home”变量，把你要的目录作为值</p></li><li><p>设置你的用户名称与邮件地址</p><pre class=" language-shell"><code class="language-shell">git config --global user.name "your name"git config --global user.email XXX@126.com# 列出当前配置项git config –list# 检查某一项配置  比如 user.namegit config user.name </code></pre></li><li><p>中文乱码：</p><pre class=" language-shell"><code class="language-shell"> git config --global core.quotepath false          # 显示status编码git config --global gui.encoding utf-8            # 图形界面编码git config --global i18n.commit.encoding utf-8    # 提交信息编码git config --global i18n.logoutputencoding utf-8  # 输出log编码# `git log` 默认使用 `less` 分页，所以需要 `bash` 对 `less` 命令进行 `utf-8` 编码export LESSCHARSET=utf-8# 让 `ls` 命令可以显示中文名称vim %GIT_HOME%\mingw64\share\git\completion\git-completion.bash# 在文件末尾处添加一行alias ls="ls --show-control-chars --color"</code></pre></li><li><p>配置ssh密钥：</p><pre class=" language-shell"><code class="language-shell">ssh-keygen -t rsa -C "XXX@126.com"</code></pre></li></ol><h2 id="2-入门使用操作-本地仓库-："><a href="#2-入门使用操作-本地仓库-：" class="headerlink" title="2. 入门使用操作(本地仓库)："></a>2. 入门使用操作(本地仓库)：</h2><ol><li>创建版本库：</li></ol><pre class=" language-shell"><code class="language-shell">#本地创建一个仓库mkdir git-opcd git-op</code></pre><ol start="2"><li>通过git init命令把这个目录变成Git可以管理的仓库：</li></ol><pre class=" language-shell"><code class="language-shell"># 在git-op目录下执行 git init# 会在当前目录下生成  .git目录  （这个目录是Git来跟踪管理版本库的，勿删！）</code></pre><ol start="3"><li>提交操作</li></ol><pre class=" language-shell"><code class="language-shell"># 在当前目录下创建一个readme.txt文件 写测试内容# 添加文件git add readme.txt # 查看文件状态 git status# 提交文件到本地仓库  -m 后面输入的是本次提交的说明，可以输入任意内容git commit -m "wrote a readme file"#查看文件具体修改内容git diff readme.txt</code></pre><ol start="4"><li> 代码回退</li></ol><pre class=" language-shell"><code class="language-shell"># 查看历史提交记录  过滤信息参数   --pretty=onelinegit log# 回退：#1. 回退到上一个版本git reset --hard HEAD^ #2. 回退到往上100个版本git reset --hard HEAD~100 #3. 如果窗口没有关闭 还可以通过版本号  回复到回退之前的状态git reset --hard +版本号#4. 如果关闭了窗口 还可以挽救  通过 git reflog (用来记录你的每一次命令) 来找回对应的commit id</code></pre><p>———————————————管理修改—————————————————————————-</p><p>工作区  (add)–&gt;  暂存区  (commit)–&gt;  master分支</p><p>每次commit 只会commit执行过add操作的文件</p><p>用 git diff HEAD – readme.txt 命令可以查看工作区和版本库里面最新版本的区别：</p><p>———————————————撤销修改—————————————————————————-</p><ol><li>撤销工作区的修改 git checkout – readme.txt     丢弃工作区的修改<pre><code> （git checkout -- file命令中的--很重要，没有--，就变成了“切换到另一个分支”的命令）  一种是readme.txt自修改后还没有被放到暂存区，现在，撤销修改就回到和版本库一模一样的状态；  一种是readme.txt已经添加到暂存区后，又作了修改，现在，撤销修改就回到添加到暂存区后的状态。</code></pre> ​<br> 总之，就是让这个文件回到最近一次git commit或git add时的状态。</li></ol><p>若想撤销工作区中所有文件的修改，则</p><p>git checkout .</p><ol start="2"><li><p>撤销暂存区的修改</p><p> git reset HEAD  <file>        可以把暂存区的修改撤销掉（unstage），重新放回工作区：</p><p> git reset HEAD readme.txt</p></li><li><p>撤销版本库的修改  版本回退</p></li></ol><p>———————————————删除文件—————————————————————————-<br>删除缓存：</p><pre><code>git rm -r -f --cached ./</code></pre><p>通过rm 删除本地文件  </p><pre><code>这时候git status 查看状态 git会提示你工作区和版本库不一致了 哪些文件被删除了两个选择:    1. 确实要删除文件：                git rm 1.txt            git commit -m &quot;delte file 1.txt&quot;            2. 误删，要找回文件：                        git checkout -- 1.txt</code></pre><h2 id="3-远程仓库连接："><a href="#3-远程仓库连接：" class="headerlink" title="3. 远程仓库连接："></a>3. 远程仓库连接：</h2><ol><li><p>添加远程库:</p><p> git remote add  origin <a href="mailto:&#103;&#105;&#x74;&#64;&#103;&#x69;&#x74;&#104;&#x75;&#x62;&#x2e;&#99;&#x6f;&#109;">&#103;&#105;&#x74;&#64;&#103;&#x69;&#x74;&#104;&#x75;&#x62;&#x2e;&#99;&#x6f;&#109;</a>:IYoreI/git-op.git            // 远程库的名字就是  origin</p><pre><code> 通过git remote 可以查看当前添加好的远程库   带参数-v 显示详细信息  git remote -v  查看当前链接情况，就是与哪些远程仓库建立了链接</code></pre><p> 删除远程库</p><pre><code> git remote rm origin</code></pre></li><li><p>把本地库的所有内容推送到远程库上:</p><p> git push -u origin master</p><p> (由于远程库是空的，我们第一次推送master分支时，加上了-u参数–<br> Git不但会把本地的master分支内容推送的远程新的master分支，还会把本地的master分支和远程的master分支关联起来，在以后的推送或者拉取时就可以简化命令)</p></li><li><p>以后只要本地作了提交，就可以通过命令  把本地master分支的最新修改推送至GitHub</p><p> git push origin master</p><pre><code> 首次上传 需要确认GitHub的Key  输入yes即可</code></pre></li><li><p>从远程仓库克隆到本地库</p><p> git clone <a href="https://github.com/IYoreI/Algorithm.git">https://github.com/IYoreI/Algorithm.git</a></p></li><li><p>更新远程代码到本地仓库</p><p> git fetch origin master 从远程的origin仓库的master分支下载代码到本地的origin master</p><p> git pull origin master</p></li></ol><p>—————————————–fork 操作—————————————————-</p><pre><code>首先将源项目fork到远程仓库  然后使用git clone 将远程仓库clone到本地仓库</code></pre><p>fork 项目后如何保持对源项目的更新</p><pre><code>源仓库--A  远程仓库--B   本地仓库--C</code></pre><ol><li><p>git remote -v 查看远程主机连接配置</p></li><li><p>添加仓库地址：</p><p>git remote add upstream url   添加源仓库A地址</p><p>git remte add origin url      添加远程仓库B地址</p></li><li><p>从上游仓库A fetch 分支和提交点，传送到本地，并会被存储在一个本地分支 upstream/master      git fetch upstream,默认会将远程所有的分支fetch下来 </p><p> git fetch upstream</p></li><li><p>git merge upstream/master</p><p> 利用fetch和merge合并upstream的master分支 本地的master分支就更新至upstream的master版本</p></li><li><p>将本地master分支推送到远程仓库B,完成项目更新</p><p> git push的一般形式为 git push &lt;远程主机名&gt; &lt;本地分支名&gt;  &lt;远程分支名&gt; ，例如 git push origin master：refs/for/master ，即是将本地的master分支推送到远程主机origin上的对应master分支， origin 是远程主机名</p><p> git push origin master</p></li></ol><p>—————————————–pull request [PR] 操作—————————————————-</p><ol><li><p>确认分支，查看分支情况</p><p> git branch -a                    *号表示当前分支</p></li><li><p>创建工作分支，在工作分支中进行开发任务</p><p> git checkout -b dev</p></li><li><p>在dev分支下对fork的项目进行开发  （分支切换）</p></li></ol><h2 id="4-分支管理："><a href="#4-分支管理：" class="headerlink" title="4. 分支管理："></a>4. 分支管理：</h2><ol><li><p>创建分支与合并分支：</p><p> （1） 创建分支dev</p><pre class=" language-shell"><code class="language-shell">git checkout -b dev          git checkout命令加上-b参数表示创建并切换，相当于以下两条命令：        $ git branch dev    $ git checkout dev</code></pre><p> （2）命令查看当前分支：</p><pre><code> git branch           查看本地分支  git branch -a  查看所有分支（包括远程仓库）</code></pre><p> （3）分支切换：</p><pre><code> git checkout master  </code></pre><p> （4）分支合并、dev分支的工作成果合并到master分支</p><pre><code> git merge dev       （git merge命令用于合并指定分支到当前分支）</code></pre><p> （5）合并完成之后就可以删除 dev分支了</p><pre><code> 删除本地分支：      git branch -d dev      删除远程分支：</code></pre></li></ol><p>　　            git push origin –delete dev  【git push origin –参数 远程分支名称】</p><pre><code>2. 解决冲突：        当我们基于同一个master版本创建出不同的分支 并且在不同分支上都对同一个文件进行了修改 这时提交时就会出现冲突</code></pre><p>​            </p><pre><code>3. 合并分支：    通常，合并分支时，如果可能，Git会用Fast forward模式，但这种模式下，删除分支后，会丢掉分支信息。        如果要强制禁用Fast forward模式，Git就会在merge时生成一个新的commit，这样，从分支历史上就可以看出分支信息    git merge --no-ff -m &quot;merge with no-ff&quot; dev4. 当在开发任务过程中，遇到紧急bug需要修复，此时你的dev分支只完成了部分开发任务不能提交，但是占用着工作区，可以使用stash命令保存现场   Git还提供了一个stash功能，可以把当前工作现场“储藏”起来            git stash   临时任务开发完成后，切换回dev分支，恢复现场：            (1) 用git stash apply恢复，但是恢复后，stash内容并不删除，你需要用git stash drop来删除；        (2) 用git stash pop，恢复的同时把stash内容也删了：                注：            你可以多次stash，恢复的时候，先用git stash list查看，然后恢复指定的stash，用命令：                                $ git stash apply stash@&#123;0&#125;5. 从master 更新一个特定的提交到dev    Git专门提供了一个cherry-pick命令，让我们能复制一个特定的提交到当前分支            git cherry-pick 4c805e2</code></pre><p>​    6. 创建远程的dev分支到本地<br>​<br>​            $ git checkout -b dev origin/dev<br>​<br>​    7. 把新建的本地分支push到远程服务器，远程分支与本地分支同名（当然可以随意起名):<br>​<br>    <code>shell     git push origin dev:dev     </code></p><ol start="8"><li>版本回退:    </li></ol><pre class=" language-shell"><code class="language-shell">#1 查看提交日志git log  #2 目标版本号  命令将版本回退：git reset --hard # 推送git push # 强推git push origin master -f</code></pre><h2 id="5-出坑指南："><a href="#5-出坑指南：" class="headerlink" title="5. 出坑指南："></a>5. 出坑指南：</h2><ol><li><p>换行符问题：</p><p> 执行git add file 时 提示  warning: LF will be replaced by CRLF in readme.txt.</p><p> 解决方法：</p><pre class=" language-shell"><code class="language-shell">$ rm -rf .git  // 删除.git  $ git config --global core.autocrlf false  //禁用自动转换 #然后重新执行$ git init    $ git add 文件名</code></pre></li><li><p>使用git push origin master向github推送代码，出现<a href="mailto:&#103;&#105;&#x74;&#x40;&#103;&#105;&#x74;&#104;&#x75;&#x62;&#46;&#99;&#x6f;&#x6d;">&#103;&#105;&#x74;&#x40;&#103;&#105;&#x74;&#104;&#x75;&#x62;&#46;&#99;&#x6f;&#x6d;</a>: Permission denied</p><p> （1）查看调试信息                     # ssh -v <a href="mailto:&#x67;&#105;&#116;&#x40;&#x67;&#105;&#116;&#104;&#117;&#98;&#46;&#99;&#x6f;&#109;">&#x67;&#105;&#116;&#x40;&#x67;&#105;&#116;&#104;&#117;&#98;&#46;&#99;&#x6f;&#109;</a><br> （2）查看~/.ssh/id_rsa.pub文件        more ~/.ssh/id_rsa.pub<br> （3） 把本机ssh-key拷贝到你github网站的ssh key里，在github的右上角edit your profile 里找到ssh key，然后add ssh key，把东西拷贝到key就可以了，title随便填</p></li><li><p>git push -u origin master   </p></li></ol><p>  ! [rejected]        master -&gt; master (fetch first)</p><p>  分析：  出现这个问题是因为github中的README.md文件不在本地代码目录中，可以通过如下命令进行代码合并</p><pre class=" language-shell"><code class="language-shell">git pull --rebase origin master#然后 git push origin master</code></pre><ol start="4"><li><p>git merge origin/master 出现  already up to date</p><p> git pull 也无用   通过git log  发现最近一次提交出现冲突</p></li></ol><h1 id="github多人协同操作记录："><a href="#github多人协同操作记录：" class="headerlink" title="github多人协同操作记录："></a>github多人协同操作记录：</h1><h2 id="1-fork项目基本操作："><a href="#1-fork项目基本操作：" class="headerlink" title="1. fork项目基本操作："></a>1. fork项目基本操作：</h2><p>​    参考： <a href="https://www.cnblogs.com/eyunhua/p/8463200.html">https://www.cnblogs.com/eyunhua/p/8463200.html</a></p><p>​    <strong>1.1 通过fetch命令拉取原仓库的更新</strong></p><ul><li><p>配置当前fork仓库的源地址：</p><pre class=" language-shell"><code class="language-shell">git remote add upstream <原仓库github地址></code></pre></li><li><p>获取原仓库的更新。使用fetch更新，fetch后会被存储在一个本地分支upstream/master上：</p><pre class=" language-shell"><code class="language-shell">git fetch upstream</code></pre></li><li><p>合并到本地分支。切换到本地master分支，合并upstream/master分支：</p><pre class=" language-shell"><code class="language-shell">git merge upstream/master</code></pre></li><li><p>这时候使用git log就能看到原仓库的更新了：</p><pre class=" language-shell"><code class="language-shell">git log</code></pre></li><li><p>如果需要自己github上的fork的仓库需要保持同步更新，执行git push进行推送:</p><pre class=" language-shell"><code class="language-shell">git push origin master</code></pre></li></ul>]]></content>
      
      
      <categories>
          
          <category> 工具 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
